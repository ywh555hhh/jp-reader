# CLAUDE.md

本仓库的全部约束与工作方式见 **[AGENTS.md](./AGENTS.md)**。

开工前请完整读两个文件：

1. `AGENTS.md` —— Definition of Done、硬禁止清单、必须停下来问人的情况
2. `docs/architecture.md` —— 19 条否命题，每条都有可执行的执法者

唯一入口命令是 `npm run gate`（= CI 跑的同一条命令）。完成后必须在回复里粘贴它的原始输出。
