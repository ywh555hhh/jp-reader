node_modules/
out/
*.vsix
.vscode/
.reports/
*.tmp            # atomicWrite 的临时文件（正常情况下不会留下）
*.tsbuildinfo

# ⚠ 本仓库只放代码 —— 数据根不进版本库（见 README「代码与数据分开」）
#    用前导 / 只忽略仓库根目录，这样 examples/minimal/ 里的示例数据不受影响。
/vocab/
/texts/
/rules_config.json
/providers_config.json
/data/
# 词典是外部数据：体积大、许可各异（JMdict 是 CC-BY-SA 4.0），不要进仓库
/dict/
/dictionaries/
